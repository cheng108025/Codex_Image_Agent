# Codex 圖片生成入口

## 專案任務

這個 repo 是可交接的角色圖片生成專案。使用者不需要知道內部 Agent、Skill 或檔案路徑。

當使用者輸入以下語意（角色名可替換）：

```text
讀取這份專案，並開始生圖流程。
請先製作 {角色名字} 的圖片。
```

必須把它視為「立即啟動指定角色的生圖工作」，不是 Git 整理、文件介紹或只提供操作教學。

## 強制路由

1. 完整讀取 `.agents/skills/story-character-image-flow/SKILL.md`。
2. 依該 Skill 自動尋找角色、檢查圖片狀態並載入新版流程。
3. 能從 repo 找到的資訊自行取得，不要求使用者再次提供路徑、專案名或流程名稱。
4. 生成第一張流程規定的身份錨點圖後停止，呈現給使用者核准。

## 單次生圖鐵律（最高優先）

- 使用者每次提出一次生圖要求，最多呼叫圖片生成工具一次，且只產出一張新圖。
- 圖片生成後立即呈現並停止；內部驗收只能回報可能的偏差，不得自行淘汰、自動修改、自動重生、補生備選圖或第二張。
- 只有使用者看過圖片後明確表示不滿意、要求「修改」「重生」或指出要改的內容，才授權下一次圖片生成；該次仍只生成一張並再次停止。
- 使用者明確要求修改時，保留上一張候選，不覆蓋既有正式 PNG；需要歸檔時才移入角色的 `rejects/`，再生成一張新候選。
- 本節優先於角色 `PROMPTS.md` 或下游 Skill 中任何「REJECT 後重生成」的舊文字；一律解讀為「標示問題並等待使用者決定」。

## 角色資料分流

- 角色資料夾已有正式 PNG：整個角色包先凍結，不改 SPEC、PROMPTS、圖片或編號；但使用者明確表示不滿意或要求修改上一張時，視為只針對該張候選的重生授權。
- 角色資料夾沒有任何正式 PNG：必須具備新版 01–07／01–06 SPEC、PROMPTS，且固定三張風格參考圖完整，通過前提檢查後再生 01。
- 尚無角色包的新角色：從故事文字開始，依三段流程直接建立 01–07／01–06。

## 安全邊界

- 不刪除、不覆蓋、不重新命名任何既有 PNG。
- 不修改任何已有正式 PNG 的角色包；需要處理時先取得使用者明確授權。
- 使用者明確拒絕或要求重生成後，上一張候選圖才放進角色的 `rejects/`。
- 不把 `DESIGN-PROPOSAL` 或 `PENDING-USER-INPUT` 擅自改成 `CANON`。
- 遇到會改變角色辨識度的阻塞型待確認事項，只詢問該項必要問題。
- 若目前 Codex 環境沒有圖片生成能力，清楚回報缺少的能力；不得假裝已產圖。
- 除非使用者另外要求，啟動生圖流程時不要 commit、push 或改寫 Git 歷史。
